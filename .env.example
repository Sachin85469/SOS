# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=visitordb
DB_USER=postgres
DB_PASSWORD=yourpassword

# Server Configuration
PORT=3000

# Feature: Admin Auth for Reset
# Set this to any secret string — only users who know this key can reset the counter
# Example: ADMIN_KEY=mysecret123
ADMIN_KEY=change-this-to-a-secret-key
